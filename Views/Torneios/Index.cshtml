@* @model IEnumerable<_8BallShot.Models.Torneio>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeTorneio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nFases)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nJogadores)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataInicio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeTorneio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nFases)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nJogadores)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataInicio)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTorneio">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTorneio">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTorneio">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
 *@


@model List<Jogador>

<h1>Select Players for Tournament</h1>

<form method="post" action="/Torneios/Bracket">
    <label for="player1">Player 1:</label>
    <select name="player1" id="player1">
        @foreach (var player in Model)
        {
            <option value="@player.IdJogador">@player.NomeJogador</option>
        }
    </select>
    <br />
    <label for="player2">Player 2:</label>
    <select name="player2" id="player2">
        @foreach (var player in Model)
        {
            <option value="@player.IdJogador">@player.NomeJogador</option>
        }
    </select>
    <br />
    <input type="submit" value="Start Tournament" />
</form>
